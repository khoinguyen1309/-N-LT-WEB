@model TachLayout.Models.ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Đã xảy ra lỗi</h1>

@if (!string.IsNullOrEmpty(Model.Message))
{
    <h3 class="text-warning">@Model.Message</h3>
}
else
{
    <h3 class="text-warning">Đã xảy ra lỗi trong quá trình xử lý yêu cầu của bạn.</h3>
}

@if (Model.ShowRequestId)
{
    <p>
        <strong>Mã lỗi (Request ID):</strong> <code>@Model.RequestId</code>
    </p>
}

<hr />

<h4>Chế độ phát triển (Development Mode)</h4>
<p>
    Khi bật chế độ <strong>Development</strong>, bạn sẽ thấy thông tin chi tiết về lỗi.
</p>
<p>
    <strong>Lưu ý:</strong> Không nên bật chế độ này trên môi trường thực tế (Production)
    vì có thể lộ thông tin nhạy cảm.
    Để bật, đặt biến môi trường:
    <br />
    <code>ASPNETCORE_ENVIRONMENT=Development</code>
</p>
